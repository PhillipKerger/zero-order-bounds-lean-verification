import ZeroOrderBounds.OracleState
import ZeroOrderBounds.ProjectionGeometry

/-!
# Public lower-bound statement vocabulary

This module exposes the definitions needed to state the fixed-horizon lower
bound without importing its proof from `ZeroOrderBounds.Main`.  Comparator's
trusted challenge imports this boundary; the production solution imports the
completed library separately.
-/
