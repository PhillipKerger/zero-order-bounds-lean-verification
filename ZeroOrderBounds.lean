import ZeroOrderBounds.Main
import ZeroOrderBounds.RepeatedQuery

/-!
# ZeroOrderBounds

Library root for the formally verified fixed-horizon lower bound for
deterministic exact-value zeroth-order convex optimization.  Importing this
module checks the complete proof chain and the repeated-query sanity theorem.
-/
